import React from 'react';

import TitleBar from '../partials/title-bar.jsx';

export default class Fleet extends React.Component {
    render() {
        return (
            <div id='#fleet-page'>
                <TitleBar>Our Fleet</TitleBar>
            </div>
        );
    }
}
